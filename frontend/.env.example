# ── Caretica Frontend — Environment Variables ──
# Copy to .env.local for local dev, set in Vercel dashboard for production

# Local dev: leave empty (Vite proxy handles /api → localhost:3001)
VITE_API_URL=

# Production: set to your Railway backend URL
# VITE_API_URL=https://caretica-backend.up.railway.app
